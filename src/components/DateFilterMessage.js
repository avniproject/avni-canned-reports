import React from 'react';

const DateFilterMessage = () => {
    return (
        <div style={{padding: '2px', backgroundColor: '#dcdcdc', width: '250px'}}>
            Date filter is not applied
        </div>
    )
};

export default DateFilterMessage;
